/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var threeSumClosest = function(nums, target) {
    const result = [];
    const numsSort = nums.sort((a, b) => a - b);
    let minDistance = 0;
    
    const changeDistance = (start) => {

    }
};